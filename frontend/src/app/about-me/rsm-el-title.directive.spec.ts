import { AboutMeResumeElementTitleDirective } from './rsm-el-title.directive';

describe('AboutMeResumeElementTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new AboutMeResumeElementTitleDirective();
    expect(directive).toBeTruthy();
  });
});
