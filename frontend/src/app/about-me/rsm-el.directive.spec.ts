import { AbtMeRsmElDirective } from './rsm-el.directive';

describe('AbtMeRsmElDirective', () => {
  it('should create an instance', () => {
    const directive = new AbtMeRsmElDirective();
    expect(directive).toBeTruthy();
  });
});
