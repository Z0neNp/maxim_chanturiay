import { RsmElTitleDirective } from './rsm-el-title.directive';

describe('RsmElTitleDirective', () => {
  it('should create an instance', () => {
    expect(new RsmElTitleDirective()).toBeTruthy();
  });
});
